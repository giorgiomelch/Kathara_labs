!
password zebra
enable password zebra
!
!
!  BGP CONFIGURATION
!
debug bgp keepalives
debug bgp updates in
debug bgp updates out
!
router bgp 3
    no bgp network import-check
    !
    ! NEIGHBORS
    !
    neighbor 10.1.3.1 remote-as 1
    neighbor 2.2.2.2 remote-as 3
    neighbor 2.2.2.2 update-source 1.1.1.1
    !
    ! NETWORKS
    !
    network 3.0.0.0/8
    network 10.1.3.0/30
    !
    ! POLICIES
    ! in=annuncia, out=accetta
    
    neighbor 10.1.3.1 prefix-list defaultRoute in
    neighbor 10.1.3.1 prefix-list defaultRoute out

    neighbor 2.2.2.2 prefix-list defaultRoute in
    neighbor 2.2.2.2 prefix-list defaultRoute out
    !
    ip prefix-list defaultRoute permit any
!
!
! FRRouting configuration file
!
!
!  RIP CONFIGURATION
!
router rip
    network 3.0.0.0/8
    redistribute bgp
!
log file /var/log/frr/frr.log