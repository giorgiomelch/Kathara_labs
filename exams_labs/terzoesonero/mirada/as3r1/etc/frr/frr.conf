!
password zebra
enable password zebra
!
!
!  BGP CONFIGURATION
!
debug bgp keepalives
debug bgp updates in
debug bgp updates out
!
router bgp 3
    no bgp network import-check
    !
    ! NEIGHBORS
    !
    neighbor 20.30.0.1 remote-as 2
    neighbor 3.0.0.2 remote-as 3
    !
    ! NETWORKS
    !
    network 3.0.0.0/8
    network 20.30.0.0/30
    !
    ! POLICIES
    ! in=annuncia, out=accetta
    !neighbor MODIFICA default-originate !se isp upstream che segnala 0.0.0.0
    neighbor 20.30.0.1 prefix-list defaultRoute in
    neighbor 20.30.0.1 prefix-list defaultRoute out

    neighbor 3.0.0.2 prefix-list defaultRoute in
    neighbor 3.0.0.2 prefix-list defaultRoute out
    !
    ip prefix-list defaultRoute permit any
!
!
! FRRouting configuration file
!
!
!  RIP CONFIGURATION
!
router rip
!send rip multicast packets to interfaces falling into this prefix
network 3.0.0.0/8
redistribute bgp
redistribute connected
!
log file /var/log/frr/frr.log